### AEチュートリアル
レンダリング
> レンダーキューを表示して出力設定を変える
> レンダリング出力

連番画像を読み込んで動画にする
> 素材に連番画像が入ったディレクトリをプロジェクトに入れると動画化できる

画像をアニメーション化する
> ツールバーのパペット(ピンマーク)
>> ピンを画像の好きな位置に打つ。
>> それを移動するキーを打てば動かすことができる。
>> 像が重なっている画像の手前だけ動かしたいなどしたい場合は、そのレイヤーごとに分けた画像素材を用意しておく必要がある
